import { resetPasswordRequestSchema } from '@healthcare/shared';

/**
 * Zod schema for validating the POST /api/v1/auth/reset-password request body.
 */
export const resetPasswordSchema = resetPasswordRequestSchema;
